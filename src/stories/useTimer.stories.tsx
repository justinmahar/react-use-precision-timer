import React from 'react';
import { UseTimerExample } from '../components/UseTimerExample';

// Needed to wrap the hook and give it visual representation.
export default {};

export const TimerStory = () => <UseTimerExample />;
TimerStory.story = {
  name: 'Hook Visual',
};
